{{- $scheme := ternary "https" "http" (or .Values.global.tls .Values.ingress.tls.enabled) -}}

CISO Assistant {{ (include "ciso-assistant.defaultTag" .) }} deployed !
==========================================
{{- if .Values.postgresql.enabled }}
WARNING : Bitnami PostgreSQL is deprecated and will be removed in a future version !
{{- end }}

Instance link : {{ printf "%s://%s" $scheme .Values.global.domain }}
