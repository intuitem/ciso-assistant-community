{{- if and .Values.backend.config.oidcProvider.existingSecret .Values.backend.config.oidcProvider.privateKey }}
  {{- fail "backend.config.oidcProvider.privateKey and backend.config.oidcProvider.existingSecret are mutually exclusive" }}
{{- end }}
{{- if not .Values.backend.config.oidcProvider.existingSecret }}
apiVersion: v1
kind: Secret
metadata:
  name: {{ include "ciso-assistant.idpOidcSecretName" . }}
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "ciso-assistant.labels" (dict "context" . "component" "backend") | nindent 4 }}
type: Opaque
data:
  idp-oidc-private-key: {{ include "ciso-assistant.idpOidcPrivateKey" . | b64enc | quote }}
{{- end }}
