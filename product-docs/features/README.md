# Feature catalogue

This section is the running catalogue of shipped product features. Each feature gets one page that serves both audiences in a single document — what it does, how end users work with it, and what implementers need to know to integrate or extend it.

## How this catalogue is maintained

- **Per pull request.** When a PR ships a new feature or meaningfully changes an existing one, the PR is expected to add or update the corresponding page here. This keeps documentation drift to a minimum because docs travel with the code.
- **From a template.** Authors copy the [feature page template](../contributing/feature-page-template.md) and fill in the sections. The template is short on purpose — feature pages should be skimmable.
- **Indexed in [`SUMMARY.md`](../SUMMARY.md).** New pages are linked from the navigation so they show up in the GitBook sidebar.

## Catalogue

> _Draft — to be populated._
>
> Initial backfill candidates (recent or in-progress features that would make good worked examples of the template):
>
> - Project management — landed in commit `e0a37ed65`.
> - Custom attributes / custom fields — current in-progress work.
> - Applied control analytics — landed in commit `40398b2b6`.
> - Auditee dashboard.
> - Batch actions on model tables.

As pages are added, list them here grouped by area (e.g. _Compliance_, _Risk_, _Third-party_, _Operations_, _Integrations_).
