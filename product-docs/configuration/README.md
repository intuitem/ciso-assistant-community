# Configuration

> This section is being expanded.

Material to come: organisation setup (users, groups, custom roles, teams), the IAM model, single sign-on (SAML, OIDC) with per-provider guides (Entra ID, Okta, Google Workspace, Keycloak), bringing content in through [libraries](libraries/README.md) and the [data import wizard](data-import.md), and changing the interface language.
