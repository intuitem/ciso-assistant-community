# Configuration

> This section is being expanded.

Material to come: organisation setup (users, groups, custom roles, teams), the IAM model, single sign-on (SAML, OIDC) with per-provider guides (Entra ID, Okta, Google Workspace, Keycloak), changing the interface language, and designing custom libraries and frameworks.
