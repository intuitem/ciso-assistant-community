name: BAD - template injection demo

on:
  pull_request:

permissions:
  contents: read

jobs:
  demo:
    runs-on: ubuntu-24.04
    steps:
      - name: BAD TEST - template expansion in run
        shell: bash
        run: |
          set -euo pipefail
          echo "PR title is: ${{ github.event.pull_request.title }}"