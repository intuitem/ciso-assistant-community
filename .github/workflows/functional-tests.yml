name: Functional Tests
permissions:
  contents: read

on:
  pull_request:
    branches: [main, develop]
    types: [opened, synchronize]
    paths:
      - "backend/**"
      - "frontend/**"
      - "enterprise/**"
      - ".github/actions/**"
      - ".github/workflows/functional-tests.yml"
  workflow_dispatch:

# Cancel older in-progress runs for the same PR or the same ref (branch)
concurrency:
  group: ${{ github.workflow }}-${{ github.event.pull_request.number || github.ref }}
  cancel-in-progress: true

env:
  BRANCH_NAME: ${{ github.head_ref || github.ref_name }}
  GITHUB_WORKFLOW: github_actions
  backend-directory: ./backend
  frontend-directory: ./frontend
  enterprise-backend-directory: ./enterprise/backend
  enterprise-frontend-directory: ./enterprise/frontend
  enterprise-frontend-build-directory: ./enterprise/frontend/.build/frontend
  enterprise-backend-settings-module: enterprise_core.settings
  DEBUG: pw:webserver
  POSTGRES_VERSION: "16"
  UBUNTU_VERSION: "24.04"
  PYTHON_VERSION: "3.14"

jobs:
  build_community_frontend:
    runs-on: ubuntu-24.04
    outputs:
      build_done: "true"
    steps:
      - name: Checkout code
        uses: actions/checkout@v6

      - name: Setup Node.js
        uses: actions/setup-node@v6
        with:
          node-version: "24"

      - name: Cache pnpm modules for Build
        uses: actions/cache@v5
        with:
          path: ${{ env.frontend-directory }}/node_modules
          key: ${{ runner.os }}-pnpm-${{ hashFiles('${{ env.frontend-directory }}/pnpm-lock.yaml') }}
          restore-keys: |
            ${{ runner.os }}-pnpm-

      - name: Install pnpm
        uses: pnpm/action-setup@v5
        with:
          version: 10

      - name: Install community frontend dependencies
        working-directory: ${{ env.frontend-directory }}
        run: pnpm i --frozen-lockfile

      - name: Build community frontend
        working-directory: ${{ env.frontend-directory }}
        env:
          NODE_OPTIONS: "--max-old-space-size=10240"
        run: pnpm run build

      - name: Upload Community Frontend Build Artifact
        uses: actions/upload-artifact@v7
        with:
          name: community-frontend-build
          include-hidden-files: true
          path: |
            ${{ env.frontend-directory }}/build/
            ${{ env.frontend-directory }}/.svelte-kit/
          retention-days: 1

  build_enterprise_frontend:
    runs-on: ubuntu-24.04
    outputs:
      build_done: "true"
    steps:
      - name: Checkout code
        uses: actions/checkout@v6

      - name: Setup Node.js
        uses: actions/setup-node@v6
        with:
          node-version: "24"

      - name: Cache pnpm modules for Build
        uses: actions/cache@v5
        with:
          path: ${{ env.enterprise-frontend-build-directory }}/node_modules
          key: ${{ runner.os }}-pnpm-${{ hashFiles('${{ env.enterprise-frontend-build-directory }}/pnpm-lock.yaml') }}
          restore-keys: |
            ${{ runner.os }}-pnpm-

      - name: Install pnpm
        uses: pnpm/action-setup@v5
        with:
          version: 10

      - name: Build enterprise frontend
        working-directory: ${{ env.enterprise-frontend-directory }}
        env:
          NODE_OPTIONS: "--max-old-space-size=10240"
        run: make

      - name: Upload Enterprise Frontend Build Artifact
        uses: actions/upload-artifact@v7
        with:
          name: enterprise-frontend-build
          include-hidden-files: true
          path: |
            ${{ env.enterprise-frontend-build-directory }}/build/
            ${{ env.enterprise-frontend-build-directory }}/.svelte-kit/
          retention-days: 1

  get_playwright_test_files:
    needs:
      - build_community_frontend
      - build_enterprise_frontend
    runs-on: ubuntu-24.04
    outputs:
      community_test_files: ${{ steps.list_community_files.outputs.test_files }}
      enterprise_test_files: ${{ steps.list_enterprise_files.outputs.test_files }}
    steps:
      - name: Checkout code
        uses: actions/checkout@v6

      - name: Get Community Playwright Test Files
        id: list_community_files
        working-directory: ${{ env.frontend-directory }}/
        run: |
          COMMUNITY_TEST_FILES=$(find tests -path tests/functional/enterprise -prune -name "*.spec.ts" -o -name "*.spec.js" -o -name "*.test.ts" -o -name "*.test.js" | jq -R -s -c 'split("\n")[:-1]')
          echo "Community Test files found: $COMMUNITY_TEST_FILES"
          echo "test_files=$COMMUNITY_TEST_FILES" >> $GITHUB_OUTPUT

      - name: Pre-build Enterprise Frontend
        working-directory: ${{ env.enterprise-frontend-directory }}
        run: make pre-build

      - name: Get Enterprise Playwright Test Files
        id: list_enterprise_files
        working-directory: ${{ env.enterprise-frontend-build-directory }}
        run: |
          ENTERPRISE_TEST_FILES=$(find tests -name "*.spec.ts" -o -name "*.spec.js" -o -name "*.test.ts" -o -name "*.test.js" | jq -R -s -c 'split("\n")[:-1]')
          echo "Enterprise Test files found: $ENTERPRISE_TEST_FILES"
          echo "test_files=$ENTERPRISE_TEST_FILES" >> $GITHUB_OUTPUT

  setup-pg-dump-community:
    runs-on: ubuntu-24.04
    env:
      DJANGO_DEBUG: "True"
      POSTGRES_NAME: postgres
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      DB_HOST: localhost
    services:
      postgres:
        image: postgres:16
        env:
          POSTGRES_USER: postgres
          POSTGRES_PASSWORD: postgres # test credential
          POSTGRES_DB: postgres
        ports: ["5432:5432"]
        options: --health-cmd pg_isready --health-interval 10s --health-timeout 5s --health-retries 5
    steps:
      - uses: actions/checkout@v6

      - name: Set up python ${{ env.PYTHON_VERSION }}
        uses: actions/setup-python@v6
        with:
          python-version: ${{ env.PYTHON_VERSION }}
          cache: "pip"

      - name: Install Poetry via pipx
        shell: bash
        run: |
          set -euo pipefail
          python -m pip install --upgrade pip
          python -m pip install --upgrade pipx
          python -m pipx ensurepath
          echo "$HOME/.local/bin" >> "$GITHUB_PATH"
          python -m pipx install poetry

      - name: Configure Poetry virtualenv in project
        run: poetry config virtualenvs.in-project true

      - name: Cache Virtualenv
        uses: actions/cache@v5
        with:
          path: ${{ env.backend-directory }}/.venv
          key: venv-${{ runner.os }}-py${{ env.PYTHON_VERSION }}-${{ hashFiles('backend/poetry.lock') }}

      - name: Install backend requirements
        working-directory: ${{ env.backend-directory }}
        run: poetry install

      - name: Restore PG dump cache
        id: pg-dump-cache
        uses: actions/cache@v5
        with:
          path: ${{ env.backend-directory }}/db/prebuilt-pg-community.sql
          key: pg-dump-community-${{ runner.os }}-${{ hashFiles('backend/**/migrations/*.py', 'backend/poetry.lock') }}

      - name: Run migrations and dump
        if: steps.pg-dump-cache.outputs.cache-hit != 'true'
        working-directory: ${{ env.backend-directory }}
        env:
          PGPASSWORD: postgres
        run: |
          poetry run python manage.py migrate
          pg_dump -h localhost -U postgres -d postgres > db/prebuilt-pg-community.sql

      - name: Upload PG dump
        uses: actions/upload-artifact@v7
        with:
          name: prebuilt-pg-dump-community
          path: ${{ env.backend-directory }}/db/prebuilt-pg-community.sql
          retention-days: 1

  setup-pg-dump-enterprise:
    runs-on: ubuntu-24.04
    env:
      DJANGO_DEBUG: "True"
      POSTGRES_NAME: postgres
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      DB_HOST: localhost
      DJANGO_SETTINGS_MODULE: enterprise_core.settings
      LICENSE_SEATS: 999
    services:
      postgres:
        image: postgres:16
        env:
          POSTGRES_USER: postgres
          POSTGRES_PASSWORD: postgres # test credential
          POSTGRES_DB: postgres
        ports: ["5432:5432"]
        options: --health-cmd pg_isready --health-interval 10s --health-timeout 5s --health-retries 5
    steps:
      - uses: actions/checkout@v6

      - name: Set up python ${{ env.PYTHON_VERSION }}
        uses: actions/setup-python@v6
        with:
          python-version: ${{ env.PYTHON_VERSION }}
          cache: "pip"

      - name: Install Poetry via pipx
        shell: bash
        run: |
          set -euo pipefail
          python -m pip install --upgrade pip
          python -m pip install --upgrade pipx
          python -m pipx ensurepath
          echo "$HOME/.local/bin" >> "$GITHUB_PATH"
          python -m pipx install poetry

      - name: Configure Poetry to not use virtualenvs
        run: poetry config virtualenvs.create false

      - name: Install backend requirements
        working-directory: ${{ env.backend-directory }}
        run: poetry install

      - name: Install enterprise backend requirements
        working-directory: ${{ env.enterprise-backend-directory }}
        run: poetry install

      - name: Restore PG dump cache
        id: pg-dump-cache
        uses: actions/cache@v5
        with:
          path: ${{ env.backend-directory }}/db/prebuilt-pg-enterprise.sql
          key: pg-dump-enterprise-${{ runner.os }}-${{ hashFiles('backend/**/migrations/*.py', 'enterprise/backend/**/migrations/*.py', 'backend/poetry.lock', 'enterprise/backend/poetry.lock') }}

      - name: Run migrations and dump
        if: steps.pg-dump-cache.outputs.cache-hit != 'true'
        working-directory: ${{ env.backend-directory }}
        env:
          PGPASSWORD: postgres
        run: |
          poetry run python manage.py migrate
          pg_dump -h localhost -U postgres -d postgres > db/prebuilt-pg-enterprise.sql

      - name: Upload PG dump
        uses: actions/upload-artifact@v7
        with:
          name: prebuilt-pg-dump-enterprise
          path: ${{ env.backend-directory }}/db/prebuilt-pg-enterprise.sql
          retention-days: 1

  functional-tests:
    needs: [get_playwright_test_files, setup-pg-dump-community]
    runs-on: ubuntu-24.04
    env:
      LOG_LEVEL: DEBUG
      LOG_OUTFILE: ciso-assistant.log
      DJANGO_DEBUG: True
      DJANGO_SUPERUSER_EMAIL: admin@tests.com
      DJANGO_SUPERUSER_PASSWORD: 1234
      POSTGRES_NAME: postgres
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      DB_HOST: localhost
      CISO_ASSISTANT_SUPERUSER_EMAIL: ""
      CISO_ASSISTANT_URL: http://localhost:4173
      DEFAULT_FROM_EMAIL: "ciso-assistant@tests.net"
      EMAIL_HOST: localhost
      EMAIL_HOST_USER: user@tests.com
      EMAIL_HOST_PASSWORD: password
      EMAIL_PORT: 1025
      CISO_ASSISTANT_VERSION: v4.2.0
      CISO_ASSISTANT_BUILD: ${{ github.sha }}
      PUBLIC_BACKEND_API_URL: http://localhost:8173/api
    services:
      postgres:
        image: postgres:16
        env:
          POSTGRES_USER: postgres
          POSTGRES_PASSWORD: postgres # test credential
          POSTGRES_DB: postgres
        ports: ["5432:5432"]
        options: --health-cmd pg_isready --health-interval 10s --health-timeout 5s --health-retries 5
      mailhog:
        image: mailhog/mailhog
        ports: ["1025:1025", "8025:8025"]

    strategy:
      max-parallel: 16
      fail-fast: false
      matrix:
        python-version: ["3.14"]
        playwright-browser: ["chromium"]
        test_file: ${{ fromJson(needs.get_playwright_test_files.outputs.community_test_files) }}

    steps:
      - uses: actions/checkout@v6

      - name: Start Keycloak
        uses: ./.github/actions/start-keycloak

      - name: Download Community Frontend Build Artifact
        uses: actions/download-artifact@v8
        with:
          name: community-frontend-build
          path: ${{ env.frontend-directory }}/

      - name: Set up python ${{ matrix.python-version }}
        uses: actions/setup-python@v6
        with:
          python-version: ${{ matrix.python-version }}
          cache: "pip"

      - name: Install Poetry via pipx
        shell: bash
        run: |
          set -euo pipefail
          python -m pip install --upgrade pip
          python -m pip install --upgrade pipx
          python -m pipx ensurepath
          echo "$HOME/.local/bin" >> "$GITHUB_PATH"
          python -m pipx install poetry

      - name: Check Poetry version
        shell: bash
        run: poetry --version

      - name: Configure Poetry virtualenv in project
        run: poetry config virtualenvs.in-project true

      - name: Cache Virtualenv
        uses: actions/cache@v5
        with:
          path: ${{ env.backend-directory }}/.venv
          key: venv-${{ runner.os }}-py${{ matrix.python-version }}-${{ hashFiles('backend/poetry.lock') }}

      - name: Install backend requirements
        working-directory: ${{ env.backend-directory }}
        run: poetry install

      - name: Setup Node.js
        uses: actions/setup-node@v6
        with:
          node-version: "24"

      - name: Cache pnpm modules
        uses: actions/cache@v5
        with:
          path: ${{ env.frontend-directory }}/node_modules
          key: ${{ runner.os }}-pnpm-${{ hashFiles('${{ env.frontend-directory }}/pnpm-lock.yaml') }}
          restore-keys: |
            ${{ runner.os }}-pnpm-

      - name: Install pnpm
        uses: pnpm/action-setup@v5
        with:
          version: 10

      - name: Install community frontend dependencies (if not cached/part of build artifact)
        working-directory: ${{ env.frontend-directory }}
        run: pnpm i --frozen-lockfile

      - name: Get installed Playwright version
        id: playwright-version
        working-directory: ${{ env.frontend-directory }}
        run: |
          PLAYWRIGHT_VERSION=$(node -p "require('@playwright/test/package.json').version")
          echo "PLAYWRIGHT_VERSION=$PLAYWRIGHT_VERSION" >> "$GITHUB_ENV"

      - name: Cache playwright binaries
        uses: actions/cache@v5
        id: playwright-cache
        with:
          path: |
            ~/.cache/ms-playwright
          key: ${{ runner.os }}-playwright-${{ env.PLAYWRIGHT_VERSION }}-${{ matrix.playwright-browser }}

      - name: Install Playwright browser ${{ matrix.playwright-browser }}
        working-directory: ${{ env.frontend-directory }}
        timeout-minutes: 20
        env:
          PLAYWRIGHT_BROWSER: ${{ matrix.playwright-browser }}
        run: |
          for attempt in 1 2 3; do
            if timeout 300 pnpm exec playwright install --with-deps "$PLAYWRIGHT_BROWSER"; then
              exit 0
            fi
            echo "::warning::Playwright install attempt $attempt failed; retrying in 15s"
            sleep 15
          done
          exit 1

      - name: Download PG dump
        uses: actions/download-artifact@v8
        with:
          name: prebuilt-pg-dump-community
          path: ${{ env.backend-directory }}/db/

      - name: Restore PG dump
        working-directory: ${{ env.backend-directory }}
        env:
          PGPASSWORD: postgres
        run: psql -h localhost -U postgres -d postgres -f db/prebuilt-pg-community.sql > /dev/null

      - name: Run migrations
        working-directory: ${{ env.backend-directory }}
        run: |
          poetry run python manage.py makemigrations
          poetry run python manage.py migrate

      - name: Start test server
        working-directory: ${{ env.backend-directory }}
        run: |
          poetry run python manage.py createsuperuser --noinput
          nohup poetry run python manage.py runserver localhost:8173 &

      - name: Run tests with browser ${{ matrix.playwright-browser }}
        working-directory: ${{ env.frontend-directory }}/
        env:
          PLAYWRIGHT_BROWSER: ${{ matrix.playwright-browser }}
          TEST_FILE: ${{ matrix.test_file }}
          CI: true
        run: pnpm exec playwright test --project="$PLAYWRIGHT_BROWSER" "$TEST_FILE"

      - name: Set current date as env variable
        if: ${{ !cancelled() }}
        run: echo "NOW=$(date +'%Y-%m-%dT%H-%M-%S')" >> $GITHUB_ENV
      - name: Sanitize branch name
        if: ${{ !cancelled() }}
        run: |
          SAFE_NAME=$(echo "$BRANCH_NAME" | sed -E 's/[^a-zA-Z0-9_-]/_/g')
          echo "BRANCH_SANITIZED=$SAFE_NAME" >> $GITHUB_ENV
      - name: Sanitize test file name
        if: ${{ !cancelled() }}
        env:
          TEST_FILE: ${{ matrix.test_file }}
        run: |
          SAFE_FILE_NAME=$(echo "$TEST_FILE" | sed -E 's/[^a-zA-Z0-9_-]/_/g')
          echo "TEST_FILE_SANITIZED=$SAFE_FILE_NAME" >> $GITHUB_ENV
      - uses: actions/upload-artifact@v7
        if: ${{ !cancelled() }}
        with:
          name: ${{ env.BRANCH_SANITIZED }}-${{ env.NOW }}-${{ github.job }}-report-${{ matrix.playwright-browser }}-${{ env.TEST_FILE_SANITIZED }}
          include-hidden-files: true
          path: |
            ${{ env.backend-directory }}/*.log
            ${{ env.frontend-directory }}/tests/results/
            ${{ env.frontend-directory }}/tests/reports/
          retention-days: 5

  enterprise-functional-tests:
    needs: [get_playwright_test_files, setup-pg-dump-enterprise]
    runs-on: ubuntu-24.04
    env:
      LOG_LEVEL: DEBUG
      LOG_OUTFILE: ciso-assistant.log
      DJANGO_DEBUG: True
      DJANGO_SUPERUSER_EMAIL: admin@tests.com
      DJANGO_SUPERUSER_PASSWORD: 1234
      POSTGRES_NAME: postgres
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      DB_HOST: localhost
      CISO_ASSISTANT_SUPERUSER_EMAIL: ""
      CISO_ASSISTANT_URL: http://localhost:4173
      DEFAULT_FROM_EMAIL: "ciso-assistant@tests.net"
      EMAIL_HOST: localhost
      EMAIL_HOST_USER: user@tests.com
      EMAIL_HOST_PASSWORD: password
      EMAIL_PORT: 1025
      CISO_ASSISTANT_VERSION: v4.2.0
      CISO_ASSISTANT_BUILD: ${{ github.sha }}
      DJANGO_SETTINGS_MODULE: enterprise_core.settings
      LICENSE_SEATS: 999
      PUBLIC_BACKEND_API_URL: http://localhost:8173/api
    services:
      postgres:
        image: postgres:16
        env:
          POSTGRES_USER: postgres
          POSTGRES_PASSWORD: postgres
          POSTGRES_DB: postgres
        ports: ["5432:5432"]
        options: --health-cmd pg_isready --health-interval 10s --health-timeout 5s --health-retries 5
      mailhog:
        image: mailhog/mailhog
        ports: ["1025:1025", "8025:8025"]

    strategy:
      max-parallel: 16
      fail-fast: false
      matrix:
        python-version: ["3.14"]
        playwright-browser: ["chromium"]
        test_file: ${{ fromJson(needs.get_playwright_test_files.outputs.enterprise_test_files) }}

    steps:
      - uses: actions/checkout@v6

      - name: Start Keycloak
        uses: ./.github/actions/start-keycloak

      - name: Download Enterprise Frontend Build Artifact
        uses: actions/download-artifact@v8
        with:
          name: enterprise-frontend-build
          path: ${{ env.enterprise-frontend-build-directory }}/

      - name: Set up python ${{ matrix.python-version }}
        uses: actions/setup-python@v6
        with:
          python-version: ${{ matrix.python-version }}
          cache: "pip"

      - name: Install Poetry via pipx
        shell: bash
        run: |
          set -euo pipefail
          python -m pip install --upgrade pip
          python -m pip install --upgrade pipx
          python -m pipx ensurepath
          echo "$HOME/.local/bin" >> "$GITHUB_PATH"
          python -m pipx install poetry

      - name: Configure Poetry to not use virtualenvs
        run: poetry config virtualenvs.create false

      - name: Check Poetry version
        shell: bash
        run: poetry --version

      - name: Install backend requirements
        working-directory: ${{ env.backend-directory }}
        run: poetry install

      - name: Install enterprise backend requirements
        working-directory: ${{ env.enterprise-backend-directory }}
        run: poetry install

      - uses: actions/setup-node@v6
        with:
          node-version: "24"

      - name: Cache pnpm modules
        uses: actions/cache@v5
        with:
          path: ${{ env.enterprise-frontend-build-directory }}/node_modules
          key: ${{ runner.os }}-pnpm-${{ hashFiles('${{ env.enterprise-frontend-build-directory }}/pnpm-lock.yaml') }}
          restore-keys: |
            ${{ runner.os }}-pnpm-

      - name: Pre-build Enterprise Frontend
        working-directory: ${{ env.enterprise-frontend-directory }}
        run: make pre-build

      - name: Install pnpm
        uses: pnpm/action-setup@v5
        with:
          version: 10

      - name: Install enterprise frontend dependencies
        working-directory: ${{ env.enterprise-frontend-build-directory }}
        run: pnpm i --frozen-lockfile

      - name: Get installed Playwright version
        id: playwright-version
        working-directory: ${{ env.enterprise-frontend-build-directory }}
        run: |
          PLAYWRIGHT_VERSION=$(node -p "require('@playwright/test/package.json').version")
          echo "PLAYWRIGHT_VERSION=$PLAYWRIGHT_VERSION" >> "$GITHUB_ENV"

      - name: Cache playwright binaries
        uses: actions/cache@v5
        id: playwright-cache
        with:
          path: |
            ~/.cache/ms-playwright
          key: ${{ runner.os }}-playwright-${{ env.PLAYWRIGHT_VERSION }}-${{ matrix.playwright-browser }}

      - name: Install Playwright browser ${{ matrix.playwright-browser }}
        working-directory: ${{ env.enterprise-frontend-build-directory }}
        timeout-minutes: 20
        env:
          PLAYWRIGHT_BROWSER: ${{ matrix.playwright-browser }}
        run: |
          for attempt in 1 2 3; do
            if timeout 300 pnpm exec playwright install --with-deps "$PLAYWRIGHT_BROWSER"; then
              exit 0
            fi
            echo "::warning::Playwright install attempt $attempt failed; retrying in 15s"
            sleep 15
          done
          exit 1

      - name: Download PG dump
        uses: actions/download-artifact@v8
        with:
          name: prebuilt-pg-dump-enterprise
          path: ${{ env.backend-directory }}/db/

      - name: Restore PG dump
        working-directory: ${{ env.backend-directory }}
        env:
          PGPASSWORD: postgres
        run: psql -h localhost -U postgres -d postgres -f db/prebuilt-pg-enterprise.sql > /dev/null

      - name: Run migrations
        working-directory: ${{ env.backend-directory }}
        env:
          SETTINGS_MODULE: ${{ env.enterprise-backend-settings-module }}
        run: poetry run python manage.py migrate --settings="$SETTINGS_MODULE"

      - name: Start test server
        working-directory: ${{ env.backend-directory }}
        env:
          SETTINGS_MODULE: ${{ env.enterprise-backend-settings-module }}
        run: |
          poetry run python manage.py createsuperuser --noinput --settings="$SETTINGS_MODULE"
          nohup poetry run python manage.py runserver localhost:8173 --settings="$SETTINGS_MODULE" &

      - name: Run tests with browser ${{ matrix.playwright-browser }}
        working-directory: ${{ env.enterprise-frontend-build-directory }}/
        env:
          PLAYWRIGHT_BROWSER: ${{ matrix.playwright-browser }}
          TEST_FILE: ${{ matrix.test_file }}
          CI: true
        run: pnpm exec playwright test --project="$PLAYWRIGHT_BROWSER" "$TEST_FILE"

      - name: Set current date as env variable
        if: ${{ !cancelled() }}
        run: echo "NOW=$(date +'%Y-%m-%dT%H-%M-%S')" >> $GITHUB_ENV
      - name: Sanitize branch name
        if: ${{ !cancelled() }}
        run: |
          SAFE_NAME=$(echo "$BRANCH_NAME" | sed -E 's/[^a-zA-Z0-9_-]/_/g')
          echo "BRANCH_SANITIZED=$SAFE_NAME" >> $GITHUB_ENV
      - name: Sanitize test file name
        if: ${{ !cancelled() }}
        env:
          TEST_FILE: ${{ matrix.test_file }}
        run: |
          SAFE_FILE_NAME=$(echo "$TEST_FILE" | sed -E 's/[^a-zA-Z0-9_-]/_/g')
          echo "TEST_FILE_SANITIZED=$SAFE_FILE_NAME" >> $GITHUB_ENV
      - uses: actions/upload-artifact@v7
        if: ${{ !cancelled() }}
        with:
          name: ${{ env.BRANCH_SANITIZED }}-${{ env.NOW }}-${{ github.job }}-report-${{ matrix.playwright-browser }}-${{ env.TEST_FILE_SANITIZED }}
          include-hidden-files: true
          path: |
            ${{ env.backend-directory }}/*.log
            ${{ env.enterprise-frontend-build-directory }}/tests/results/
            ${{ env.enterprise-frontend-build-directory }}/tests/reports/
          retention-days: 5
