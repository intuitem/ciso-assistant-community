# The repo root applies `* text=auto eol=lf`. Store dist/index.js verbatim so
# the committed file stays byte-identical to the reproducible ncc build
# (sha256 b0805f1080ae56759f93ca0bf76adca06d9c1a0d71d0d3a755c0139767f89450)
# and so the dist-check CI rebuild diffs cleanly.
dist/index.js -text
