subject: "CISO Assistant: Validation '${validation_ref_id}' has been ${new_status}"
body: |
  Hello,

  The validation request '${validation_ref_id}' has been updated:

  - New status: ${new_status}
  - Updated by: ${actor_name}
  - Domain: ${folder_name}

  ${notes_section}

  View the validation request:
  ${validation_url}

  Thank you.
