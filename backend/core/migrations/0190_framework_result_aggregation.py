# Generated by Django 6.1.1 on 2026-09-21 17:48

from django.db import migrations, models


class Migration(migrations.Migration):
    dependencies = [
        ("core", "0189_evidencerevision_original_filename"),
    ]

    operations = [
        migrations.AddField(
            model_name="framework",
            name="result_aggregation",
            field=models.CharField(
                choices=[
                    ("per_answer", "Per answer"),
                    ("tiered_all", "Tiered, all statements of a tier must hold"),
                ],
                default="per_answer",
                help_text="How questionnaire answers combine into a requirement result. 'per_answer' lets each answer stand on its own, so answers that disagree land on partially compliant; 'tiered_all' groups the statements into tiers and awards a tier only when all of it holds.",
                max_length=20,
                verbose_name="Result aggregation",
            ),
        ),
    ]
