[project]
name = "ciso-assistant-cli"
version = "0.1.0"
description = "CISO Assistant CLI tools"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "click>=8.1.0",
    "icecream>=2.1.0",
    # 1.6.0 floor predates streamable_http_app/TransportSecuritySettings
    "mcp[cli]>=1.28.1",
    "python-dotenv>=1.0.0",
    "requests>=2.32.3",
    "rich>=13.0.0",
    "uvicorn>=0.30.0",
]

[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[tool.setuptools]
py-modules = ["ca_mcp", "clica"]
